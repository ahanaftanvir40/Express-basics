<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Profile</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body>
        <div class="w-full h-screen p-10 bg-zinc-900 text-white">
            <div class="flex justify-end">
                <a class="px-3 py-2 bg-red-500 rounded-md"
                    href="/logout">Logout</a>
            </div>
            <h3 class="text-2xl mb-5"><span class="text-light">Hello, </span><%= post.user.username %>
                 👋</h3>
            <h5 class="mb-5">You can Edit this post.</h5>

            <form action="/update/<%= post._id %>" method="post">
                <textarea
                    class="block w-1/3 p-3 resize-none bg-transparent border-2 border-zinc-700 rounded-md"
                    name="content"
                    placeholder=""><%= post.content %></textarea>
                <input
                    class="block mt-3 w-52 px-5 py-2 rounded-md text-black bg-yellow-500 cursor-pointer"
                    type="submit" value="Update Post">
            </form>


            </div>
        </div>
    </body>
</html>