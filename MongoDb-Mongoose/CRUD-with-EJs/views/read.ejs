<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
        <link rel="stylesheet" href="/stylesheets/style.css">
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body>
        <div class="w-full h-full bg-zinc-800 p-10 text-white ">
            <a class="text-xl block mb-10 text-blue-500" href="/">Go back To
                Home</a>
            <h1 class="mb-3 tracking-tighter text-3xl">All Users</h1>
            <div class="users flex gap-3 flex-wrap">
                <% if(users.length > 0){%>
                <% users.forEach(function(user){%>
                <div class="user w-72 p-4 bg-zinc-500 rounded-lg">
                    <div
                        class="image w-full h-72 rounded-lg bg-zinc-400 overflow-hidden">
                        <img class="w-full h-full object-cover"
                            src="<%= user.image %>" alt>
                    </div>
                    <h3
                        class="text-xl tracking-tighter mt-3"><%=user.name%></h3>
                    <h5 class="text-zinc-300"><%=user.email%></h5>
                    <div class="flex gap-3 mt-10">
                        <a class="text-zinc-200" href>Edit User</a>
                        <a class="text-red-500"
                            href="/delete/<%=user._id%>">Delete User</a>
                    </div>

                </div>
                <%}) %>
                <%} else{%>
                <h3 class="text-zinc-400">No users yet.</h3>
                <%} %>

            </div>
        </div>
    </body>
</html>